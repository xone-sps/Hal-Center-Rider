import 'package:flutter/material.dart';

Widget appBarWidget(){
  return AppBar(
    elevation: 0.0,
    centerTitle: true,
    title: Text('Hal App Delivery',style: TextStyle(color: Colors.white),),
  );
}